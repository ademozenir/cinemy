import 'package:flutter/cupertino.dart';

class TrendingTvShowsView extends StatelessWidget {
  const TrendingTvShowsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
          child: Text("Bar Item Page"),
        ));
  }
}
