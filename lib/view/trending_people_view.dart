

import 'package:flutter/material.dart';

class TrendingPeopleView extends StatelessWidget {
  const TrendingPeopleView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
          child: Text("My Page"),
        ));
  }
}
